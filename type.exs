[
  # find these files
  only: [
    "lib/example/*.ex",
    "lib/ex_type.ex"
  ],

  # exclude these files
  # except: ["lib/*.ex", "lib/ex_type/**/*.ex", "lib/mix/**/*.ex"],

  # except: ["file.ex", Enum, {Enum, :map}, {Enum, :map, 2}]

  # add some extra typespecs for this project
  typespecs: %{}
]
